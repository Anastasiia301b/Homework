let x = 'kg'; /
let amount =7;

switch (x) {
    case 'km':
        console.log(amount + 'км це' + amount * 1000 + 'м');
        break;
    case 'hour':
        console.log(amount + 'год це' + amount * 60 + 'хв');
        break;
    case 'kg':
        console.log(amount + ' кг це ' + amount * 1000 + 'г');
        break;
    default:
        console.log('Перевірте правильність заповнення полів');
}
